program lista;
uses crt;
var
   nombre:string;
begin
     clrscr;
     writeln('Ingrese un nombre');
     readln(nombre);
     if nombre='Kevin' then
     begin
          writeln('Es el mejor hijo del mundo es:',nombre);
          end;
          readln;
end.
